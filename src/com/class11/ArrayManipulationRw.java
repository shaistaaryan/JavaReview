package com.class11;

public class ArrayManipulationRw {

	public int largestNumber(int[] array) {
		int largest = array[0];
		for(int i =0; i < array.length; i++) {
		if(array[i] > largest);
		  largset
		}	
      
	
       return largest;
   
       public static void main(String[] args) {
	   int[] arry = {5, 2, 9, 6, 1, 8};
	   
	   ArrayManipulationRw main =new ArrayManipulationRw();
	   int largest = manip.largestNumber(array);
	   System.out.println("Largest is " + largest);
	   
	   System.out.println("samllest is  " + manip.smallestNumber(arr));
	   
	   double avg = findAverage(arr);
	   System.out.println("average is" + avg);
	   
	   //HW
	   //create a method that returns sum of an array
	   	   
   }

       public int largestNumber(int[]array) {
    	   
    	   int smallestNumber(int[]array) {
    		   
    		   //evven if  parivate it can be called frome inside the calss
    		   private static double findAvergage(int[] array) {
    			   for (int num : array) {
    				   sum += num;
    			   }
    		   }
    	   }
       }
	
	
	
	return sum /array.length;
	
	
	
	
	
	
	}
}